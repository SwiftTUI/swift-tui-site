// The two page grounds, for the <meta name="theme-color"> tags that cannot
// read a stylesheet. They mirror `--page` in src/styles/tokens.css; the
// scripts/docc-theme.test.ts drift check keeps them equal.
export const pageLight = "#fffffe";
export const pageDark = "#111216";

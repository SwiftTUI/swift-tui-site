{
  "name": "swift-tui-website",
  "type": "module",
  "version": "0.9.4",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "prepare:webexample": "bun run scripts/prepare-webexample.ts",
    "compose:webexample": "bun run scripts/compose-webexample.ts",
    "compress:wasm": "bun run scripts/minify-webexample-wasm.ts --quality 11",
    "compress:wasm:dev": "bun run scripts/minify-webexample-wasm.ts --quality 9",
    "build:wasm": "bun run prepare:webexample && bun run --cwd \"${WEBEXAMPLE_DIR:-../.build/public-inputs/swift-tui-counter-demo/WebExample}\" build && bun run compress:wasm",
    "build:wasm:dev": "bun run prepare:webexample && bun run --cwd \"${WEBEXAMPLE_DIR:-../.build/public-inputs/swift-tui-counter-demo/WebExample}\" build:dev && bun run compress:wasm:dev",
    "build:docc": "../Scripts/build_docc_site.sh",
    "build:full": "bun run build:wasm && bun run build && bun run compose:webexample && bun run build:docc",
    "build:dev": "bun run build:wasm:dev && bun run build && bun run compose:webexample && bun run build:docc",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "test": "bun test"
  },
  "dependencies": {
    "@astrojs/check": "0.9.10",
    "@swifttui/web": "https://github.com/SwiftTUI/swift-tui-web/releases/download/0.9.4/swifttui-web-0.9.4.tgz",
    "astro": "7.2.2",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}

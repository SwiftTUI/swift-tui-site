{
  "name": "swift-tui-website",
  "type": "module",
  "version": "0.0.13",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "prepare:webexample": "bun run scripts/prepare-webexample.ts",
    "compose:webexample": "bun run scripts/compose-webexample.ts",
    "compress:wasm": "bun run scripts/minify-webexample-wasm.ts --quality 11",
    "compress:wasm:dev": "bun run scripts/minify-webexample-wasm.ts --quality 9",
    "build:wasm": "bun run prepare:webexample && bun run --cwd \"${WEBEXAMPLE_DIR:-../.build/public-inputs/swift-tui-examples/WebExample}\" build && bun run compress:wasm",
    "build:wasm:dev": "bun run prepare:webexample && bun run --cwd \"${WEBEXAMPLE_DIR:-../.build/public-inputs/swift-tui-examples/WebExample}\" build:dev && bun run compress:wasm:dev",
    "build:docc": "../Scripts/build_docc_site.sh",
    "build:full": "bun run build:wasm && bun run build && bun run compose:webexample && bun run build:docc",
    "build:dev": "bun run build:wasm:dev && bun run build && bun run compose:webexample && bun run build:docc",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "test": "bun test"
  },
  "dependencies": {
    "@astrojs/check": "0.9.9",
    "astro": "6.3.6",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13"
  }
}
